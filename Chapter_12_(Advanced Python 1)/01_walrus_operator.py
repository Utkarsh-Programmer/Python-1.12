# Walrus  Operator
# denoted by `:=` symbol.
# used to assign a variable within an expression.

if n := len([1, 2, 3, 4, 5]):
    print("List is too long ({n}) elements, expected less than 3.")
