def greet(name):
    return f"Hello {name}."


a = greet(name="Utkarsh")
print(a)

# NOTE: `return` statement instructs the function to stop execution and return a value or nothing to the caller.
