# Virtual Environment
# pip install virtualenv --> install virtual environment
# virtualenv new_project --> create a new venv
# pip freeze > requirements.txt --> return all the python installed packages along with their versions
# pip install -r requirements.txt --> install all the packages in in requirements.txt with their pre-defined version
