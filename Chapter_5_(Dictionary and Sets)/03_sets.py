# Sets are created using `curly braces`{1,2,3,4,5} with comma separated elements in it.
# But empty dictionaries are created using empty `curly braces`{}.
s = {1, 2, 3, 4, 5}

e = set()  # empty set

# Elements can't be repeated in sets.
n = {1, 2, 3, 3, 3, 4, 5}
print(n)
